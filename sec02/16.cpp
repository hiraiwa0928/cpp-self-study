#include <bits/stdc++.h>
using namespace std;

#define rep(n) for (int i = 0; i < n; i++)
#define range(a, b) for (int i = a; i < b; i++)

using ll = long long;

int main() {
    string s;

    cout << "文字列を入力してください> ";

    getline(cin, s);

    cout << "入力された文字列は\"" << s << "\"です。" << endl;

    return 0;
}
