#include <bits/stdc++.h>
using namespace std;

#define rep(n) for (int i = 0; i < n; i++)
#define range(a, b) for (int i = a; i < b; i++)

using ll = long long;

#define HOGE

int main() {

    #if defined(HOGE)
        cout << "defined(HOGE)はtrueです。" << endl;
    #else
        cout << "defined(HOGE)はfalseです。" << endl;
    #endif

    return 0;
}
