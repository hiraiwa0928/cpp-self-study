#include <bits/stdc++.h>
using namespace std;

#define rep(n) for (int i = 0; i < n; i++)
#define range(a, b) for (int i = a; i < b; i++)

using ll = long long;

int main() {
    
    int array[] = {0, 1, 2, 3};

    int *ptr = array;

    cout << ptr[0] << endl;
    cout << ptr[1] << endl;
    cout << ptr[2] << endl;
    cout << ptr[3] << endl;

    return 0;
}
